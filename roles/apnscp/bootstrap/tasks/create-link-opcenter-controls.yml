---
- name: Link resources
  file:
    state: touch
    path: "{{apnscp_root}}/{{src}}"
    owner: "{{apnscp_system_user}}"
- file: 
    state: link 
    path: "/etc/{{path}}" 
    src: "{{apnscp_root}}/{{src}}"
    owner: "{{apnscp_system_user}}"
  when: path != ""
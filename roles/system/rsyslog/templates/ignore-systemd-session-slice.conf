if $programname == "systemd" and ($msg startswith "Removed session " or $msg startswith "New session c" or $msg startswith "Starting Session" or $msg startswith "Started Session" or $msg startswith "Created slice" or $msg startswith "Starting user-" or $msg startswith "Removed slice user-" or $msg startswith "Stopping user-") then ~
if $programname == "systemd-login" and ($msg startswith "Removed session " or $msg startswith "New session c") then ~

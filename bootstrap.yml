---
- hosts: localhost
  become: yes
  connection: local
  gather_facts: yes
  vars_files:
    - 'apnscp-vars.yml'
  roles:      
    - filesystem/make-mounts
    - epel
    - packages
    - pgsql/install
    - mysql/install
    - browscap
    - apnscp/bootstrap
    - php/install  
    - php/install-pecl-module
    - php/create-configuration  
    - java/tomcat
    - apache/configure
    - apnscp/build-php
    - apnscp/install-extensions  
    - apnscp/initialize-db
    - apnscp/install-vendor-library
    - apnscp/initialize-filesystem-template
    # Admin should work fine at this point
    - apnscp/assert-admin-works
    - fail2ban/whitelist-self
    #- apnscp/register-ssl
    #- network/setup-firewall
    #- fail2ban/configure-jails
    #- apnscp/create-admin
    


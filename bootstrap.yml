---
- hosts: localhost
  become: yes
  gather_facts: yes
  vars_files:
    - 'apnscp-vars.yml'
  roles:
    # - apnscp-bootstrap
    #- epel
    #- packages
    #- pgsql
    # - mysql
    #- browscap
    #- apnscp-build-php
    # TODO Build apnscp php
    - apnscp-install-extensions